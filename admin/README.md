# ShadowPay Admin View (Next.js)

Not yet scaffolded — build order item 7.

Platform-wide aggregates only (total agreements, default rate, pool
utilization). Onboarding queue, pool monitoring, risk threshold config.
Deliberately cannot drill into individual user/merchant/vendor purchase data.
