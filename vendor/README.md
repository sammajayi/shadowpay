# ShadowPay Vendor View (Next.js)

Not yet scaffolded — build order item 6.

Aggregate roster stats across merchants under this vendor only. No drill-down
into any individual merchant's or user's data. Weakest Wave 1 priority per the
scope doc's cut list — can be a stub with hardcoded roster data if time is short.
