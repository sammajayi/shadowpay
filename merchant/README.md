# ShadowPay Merchant View (Next.js)

Not yet scaffolded — build order item 5.

Own agreements/payouts only. Enforced via merchant-scoped API keys AND
circuit-level decryption scoping (not just app-layer access control) — a
merchant cannot decrypt another merchant's agreement data even with the API key.
