DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=shadowpay
DB_USER=shadowpay
DB_PASSWORD=shadowpay
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:3004,http://localhost:3005,http://localhost:3006,http://localhost:3007,http://localhost:3008,http://localhost:3009

WALLET_CHALLENGE_TTL_SECONDS=300

# Generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AGREEMENT_ENCRYPTION_KEY=

DEFAULT_ELIGIBILITY_THRESHOLD=60

RESEND_API_KEY=
NOTIFICATIONS_FROM_EMAIL=notifications@shadowpay.dev
